/* ═══════════════════════════════════════════════════════════════════
   НАСТРОЙКИ ОБЪЕКТА — ЕДИНСТВЕННЫЙ ФАЙЛ, КОТОРЫЙ ВЫ РЕДАКТИРУЕТЕ

   index.html не трогайте: при обновлении приложения он заменяется,
   а этот файл остаётся — ваши настройки не потеряются.
   ═══════════════════════════════════════════════════════════════════ */

/* 1. ОБЩАЯ БАЗА НА ЯНДЕКС.ДИСКЕ.
   Строку выдаёт само приложение: раздел «Доступ и журнал» →
   «⚙ Настройка Диска (для администратора)». Скопируйте её оттуда
   и замените строку ниже целиком. Пока null — каждый работает
   автономно, общих данных нет. */
y0__wgBELaqzNYIGNuWAyDb3qLQGDC9qszWCE-p0vifmO-FECca53bZSBZVf1OV

/* 2. ДОСТУП К АНАЛИТИКЕ (темп, прогноз, деньги, план недели).
   Строку выдаёт кнопка «⚙ Расценки и доступ к аналитике».
   Пока null — аналитику видят все, кто открыл ссылку. */
window.SS_ANALYTICS = null;

/* 3. ЛИЦЕНЗИЯ (для коммерческой поставки). Обычно не нужна. */
window.SS_LICENSE = null;
